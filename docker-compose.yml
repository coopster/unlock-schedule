services:
  unlock-schedule:
    image: unlock-schedule:${APP_VERSION:-latest}
    container_name: unlock-schedule
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./secrets:/secrets:ro
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
